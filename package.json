{
	"$schema": "https://www.schemastore.org/package.json",
	"name": "corpus-monorepo",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build": "pnpm -r run build",
		"test": "pnpm -r run test:all",
		"fm": "oxfmt --write",
		"lint": "oxlint --fix",
		"changeset": "changeset",
		"version": "changeset version",
		"release": "changeset publish"
	},
	"devDependencies": {
		"@changesets/cli": "^2.30.0",
		"@types/bun": "^1.3.12",
		"oxfmt": "^0.45.0",
		"oxlint": "^1.60.0"
	}
}
